<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
      <% include ./partials/_header %>
  </header>
    <table>
      <thead>
        <tr>
          <th>URL</th>
          <th>Short URL</th>
          <th</th>
        </tr>
      </thead>
      <tbody>
        <% for (shortURL in urls) { %>
        <tr>
          <td><%= urls[shortURL].url %></td>
          <td><%= shortURL %></td>
          <td>
            <a href='/urls/<%= shortURL %>'> Edit </a>
          </td>
          <td>
            <form method ='POST' action='/urls/<%= shortURL %>/delete'>
              <input type='submit' value='Delete'/>
            </form>
          </td>
        </tr>
        <% } %>
        <tr>
          <td colSpan='2'>
            <form action='/urls/new/' >
              <input type='submit' value='Add new TinyUrl'/>
            </form>
          </td>
        </tr>
    </table>
</body>
</html>
